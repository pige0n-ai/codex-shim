# ── Moonshot / Kimi OpenAI-Compatible Chat Completions ────────────
# Codex-facing: Responses  │  Upstream: Chat Completions (adapter-internal shim)
server:
  listen: "127.0.0.1:8787"
upstream:
  base_url: "https://api.moonshot.ai/v1"
  api_key_env: "MOONSHOT_API_KEY"
  max_retries: 2
  stream_max_retries: 2
  downstream_heartbeat_seconds: 30
provider:
  kind: moonshot-chat
  profile_config:
    profile: moonshot-chat
    extra_body:
      # Kimi exposes thinking via an extra request body field.
      thinking:
        type: disabled
models:
  default: "kimi-k2.6"
  catalog:
    - slug: "kimi-k2.6"
      context_window: 262144
      apply_patch_tool_type: freeform
state:
  backend: memory
  failed_debug_artifact_ttl_seconds: 0
logging:
  level: info
